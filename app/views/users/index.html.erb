<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<!-- See notes (1) for info about the code below -->
	<% @users.each do |user| %>
		<%= render 'user', :user => user %>
	<% end %>
</ul>

<%= will_paginate %>




